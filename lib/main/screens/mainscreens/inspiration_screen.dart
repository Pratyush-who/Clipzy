import 'package:flutter/material.dart';

class InspirationScreen extends StatelessWidget {
  const InspirationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inspiration', style: TextStyle(fontSize: 24)),
    );
  }
}
